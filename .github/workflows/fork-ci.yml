name: Fork CI

on:
  push:
    branches: [main, 'sid/**']
  pull_request:
    branches: [main]

concurrency:
  group: fork-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-macos:
    name: Build macOS (LAB)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Strip non-version tags from ghostty
        run: git -C ghostty tag -l 'xcframework-*' | xargs git -C ghostty tag -d 2>/dev/null || true

      - name: Install Zig
        run: bash ./scripts/install-zig-ci.sh 0.15.2

      - name: Build GhosttyKit.xcframework
        run: |
          cd ghostty
          zig build -Demit-xcframework=true -Dxcframework-target=native -Doptimize=ReleaseFast -Demit-macos-app=false -Demit-exe=false -Demit-docs=false -Demit-webdata=false
          ln -sf "$PWD/macos/GhosttyKit.xcframework" ../GhosttyKit.xcframework
          echo "GhosttyKit.xcframework built from source ($(git rev-parse --short HEAD))"
          ls -la ../GhosttyKit.xcframework/

      - name: Build libctap2
        run: |
          cd vendor/ctap2
          zig build -Doptimize=ReleaseFast
          cp zig-out/lib/libctap2.a ../../
          cp include/ctap2.h ../../

      - name: Run zig-ctap2 tests
        run: |
          cd vendor/ctap2
          zig build test
          zig build test-pbt

      - name: Build cmux LAB (Debug)
        run: |
          xcodebuild -project GhosttyTabs.xcodeproj \
            -scheme cmux \
            -configuration Debug \
            -destination 'platform=macOS' \
            ARCHS=arm64 \
            -derivedDataPath /tmp/cmux-fork-ci \
            ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon-Fork \
            build

      - name: Build cmux LAB (Release)
        run: |
          xcodebuild -project GhosttyTabs.xcodeproj \
            -scheme cmux \
            -configuration Release \
            -destination 'platform=macOS' \
            ARCHS=arm64 \
            -derivedDataPath build \
            ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon-Fork \
            build

  nix-check:
    name: Nix flake check
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Strip non-version tags from ghostty
        run: git -C ghostty tag -l 'xcframework-*' | xargs git -C ghostty tag -d 2>/dev/null || true

      - uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: "sandbox = relaxed"
      - uses: DeterminateSystems/flakehub-cache-action@v3

      - name: Verify dev shell
        run: nix develop --command bash -c 'echo "zig $(zig version) — dev shell OK"'

      - name: Run headless flake checks (required)
        run: |
          nix build .#checks.x86_64-linux.basic-version-check --print-build-logs
          nix build .#checks.x86_64-linux.cmux-linux-build-check --print-build-logs
          nix build .#checks.x86_64-linux.gtk4-version-check --print-build-logs

      - name: Run socket test suite check (best-effort)
        continue-on-error: true
        timeout-minutes: 45
        run: nix build .#checks.x86_64-linux.socket-test-suite --print-build-logs

      - name: Run graphical flake check (best-effort)
        continue-on-error: true
        timeout-minutes: 30
        run: nix build .#checks.x86_64-linux.basic-window-check-gnome --print-build-logs
